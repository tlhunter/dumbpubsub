GET: <?=$_GET['type']?>

<pre><?php print_r($_POST); ?></pre>

This is a simple script to show the results of a POST operation. It could be
something more complex like running important scripts, updating cache, or
sending emails.
